# ============================================
# PRODUCTION CONFIGURATION (EC2 + RDS)
# ============================================
# Copy this to .env on your EC2 instance and fill in actual values

# Server
NODE_ENV=production
PORT=5000

# Database (RDS PostgreSQL)
# Format: postgresql://USERNAME:PASSWORD@RDS_ENDPOINT:5432/DATABASE_NAME?schema=public
# Example: postgresql://cpmsuser:YourPassword123@cpms-db.abc123.us-east-1.rds.amazonaws.com:5432/cpmsdb?schema=public
DATABASE_URL=postgresql://YOUR_RDS_USER:YOUR_RDS_PASSWORD@YOUR_RDS_ENDPOINT:5432/YOUR_DB_NAME?schema=public

# Redis (running on same EC2 instance)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT Security
# Generate a strong 32+ character secret: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_32_CHARS_MINIMUM
JWT_EXPIRES_IN=7d

# CORS - Add your Vercel frontend URLs (comma-separated)
# Example: https://cpms.vercel.app,https://www.yourdomain.com
CORS_ORIGIN=https://YOUR_PROJECT.vercel.app,https://www.YOUR_CUSTOM_DOMAIN.com

# Cloudinary (file storage)
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name
CLOUDINARY_API_KEY=your-cloudinary-api-key
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# Super Admin Bootstrap (for first-time setup only)
# These are used by the createSuperAdmin script
# Remove or comment out after creating the first super admin
SUPER_ADMIN_NAME=Your Full Name
SUPER_ADMIN_EMAIL=admin@yourdomain.com
SUPER_ADMIN_PASSWORD=YourStrongPassword123!
