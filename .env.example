# Environment
NODE_ENV=development

# ============================================
# DOCKER PORTS (used when running with docker-compose)
# ============================================
DOCKER_API_PORT=4001          # Backend API external port (maps to internal 4000)
DOCKER_FRONTEND_PORT=3001     # Frontend external port (maps to internal 3000)
DOCKER_POSTGRES_PORT=5433     # PostgreSQL external port (maps to internal 5432)
DOCKER_REDIS_PORT=6380        # Redis external port (maps to internal 6379)
DOCKER_NEXT_PUBLIC_API_URL=http://localhost:4001/api/v1

# ============================================
# DATABASE & REDIS CONFIG (for Docker containers)
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=cpms

# ============================================
# LOCAL DEVELOPMENT (used when running npm run dev)
# See backend/.env.example and frontend/.env.example
# Backend runs on: 4000
# Frontend runs on: 3000
# PostgreSQL runs on: 5432
# Redis runs on: 6379
# ============================================

# JWT (change in production!)
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3001

# Cloudinary (for document uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
