/**
 * Common Components
 * 
 * This file exports all reusable components for the CPMS design system.
 * Import components from '@/components/common' to ensure visual consistency.
 */

// Layout components
export { InstitutionalNavbar, AppHeader, AppNav, AppFooter, PageContainer, PageTitle, SectionTitle } from './Layout';

// Card components
export { Card, CardHeader, StatCard, InfoRow, Grid, FilterTabs } from './Cards';

// Table components
export { DataTable, SimpleTable, TableRow, TableCell } from './Table';

// Button components
export { Button, IconButton, LinkButton, TableActionLink } from './Button';

// Status badge
export { StatusBadge, DotIndicator } from './StatusBadge';

// Empty and loading states
export { EmptyState, LoadingState, PageLoading } from './EmptyState';

// Form components
export { Input, Textarea, Select, Checkbox, FormGroup, FormRow, FormSection, FileUpload } from './Form';
